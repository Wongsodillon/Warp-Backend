spring.application.name=warp-backend
server.port=8080

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://moving-moose-5.clerk.accounts.dev/.well-known/jwks.json

application.short-url.secret=${SHORT_URL_SECRET}
application.domain.url=${APPLICATION_DOMAIN_URL}